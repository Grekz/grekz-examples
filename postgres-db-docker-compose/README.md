# postgres-db-docker-compose

Start:

```bash
$ docker-compose up
```

Stop:

```bash
$ docker-compose down
```

List docker volumes:

```bash
$ docker volume ls
```

Remove volume:

```bash
docker volume rm postgres-db-docker_db-data-example
```
